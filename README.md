Updated.xml is the uppaal model.  
filter3classfiles is one python code generated to help labeling 3 classes in the svm: straight, left, right.  
lanefollowingopencv is one python code uses the input of dataset to create x_features1.npy and y_lebels1.npy.  
the two files will be used in svm training model to train the model , the output is svm_lane_modeljob.joblib
